import { PlaceholderPage } from './PlaceholderPage';

export function AddProblemPage() {
  return (
    <PlaceholderPage
      eyebrow="New problem"
      title="Add Problem"
      subtitle="Students will scan, type, or build a formula before Pico extracts numbers, signs, units, and formulas."
    />
  );
}
