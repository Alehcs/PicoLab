import { PlaceholderPage } from './PlaceholderPage';

export function ProfilePage() {
  return (
    <PlaceholderPage
      eyebrow="Profile"
      title="Profile"
      subtitle="Compact summary with lower tabs for subjects, progress, achievements, history, and Pico quotes."
    />
  );
}
